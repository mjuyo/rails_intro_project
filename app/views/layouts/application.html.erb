<!DOCTYPE html>
<html>
  <head>
    <title>Parks and Trees in Winnipeg</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-800 text-slate-200">
    <nav class="bg-gray-700 p-4 text-white">
      <div class="container mx-auto flex justify-between items-center">
        <div class="flex space-x-4">
          <%= link_to 'Parks&Assets', root_path, class: "text-2xl" %>
          <%= link_to 'Parks', parks_path, class: 'hover:text-blue-500' %>
          <%= link_to 'Trees', trees_path, class: 'hover:text-blue-500'  %>
          <%= link_to 'Assets', assets_path, class: 'hover:text-blue-500'  %>
          <%= link_to 'About', about_path, class: 'hover:text-blue-500'  %>
        </div>

        <div class="flex space-x-2">
          <form action="<%= search_path %>" method="get">
            <input type="text" name="query" placeholder="Search parks..." class="text-gray-900 p-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
            <select name="nbhd" class="text-gray-900 p-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
              <option value="">All Neighborhoods</option>
              <% Park.distinct.order(:nbhd).pluck(:nbhd).uniq.each do |nbhd| %>
                <option value="<%= nbhd %>"><%= nbhd %></option>
              <% end %>
            </select>
            <input type="submit" value="Search"class="p-2 bg-blue-500 text-white rounded cursor-pointer hover:bg-blue-600" >
          </form>
        </div>
      </div>
    </nav>
      
    <div class="container mx-auto p-4">
      <%= yield %>
    </div>

  </body>
</html>

